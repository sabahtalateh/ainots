# Теорема о пределе суммы

## Формулировка

**Теорема:** Если $\lim_{n \to \infty} a_n = A$ и $\lim_{n \to \infty} b_n = B$, то:

$$\lim_{n \to \infty} (a_n + b_n) = A + B$$

**Простыми словами:** Предел суммы равен сумме пределов.

---

<details>
<summary><b>📖 Что значит "близко" и "стремится"?</b></summary>

Перед доказательством важно понять **точные** определения этих интуитивных слов.

### Бытовое понимание (неточное)

**"Близко"** — рядом, почти одинаково  
**"Стремится"** — приближается, становится всё ближе

Но в математике нужна **абсолютная точность**!

### Математическое определение "близко"

**"Число $x$ близко к числу $A$"** означает:

$$|x - A| < \varepsilon$$

где $\varepsilon$ (эпсилон) — это **насколько близко** мы хотим.

**Примеры:**

1. $x = 3.14$ близко к $\pi \approx 3.14159$ с точностью $\varepsilon = 0.01$?
   - $|3.14 - 3.14159| = 0.00159 < 0.01$ ✓ Да!

2. $x = 3.1$ близко к $\pi$ с точностью $\varepsilon = 0.001$?
   - $|3.1 - 3.14159| = 0.04159 > 0.001$ ✗ Нет!

**Ключевая идея:** "Близко" — это не абсолютное понятие, а **относительное**. Зависит от выбранного $\varepsilon$.

---

### Математическое определение "стремится"

**"Последовательность $a_n$ стремится к $A$"** означает:

> Для **любой** наперёд заданной точности $\varepsilon > 0$ найдётся такой номер $N$, что **все** члены последовательности с номерами больше $N$ находятся на расстоянии меньше $\varepsilon$ от $A$.

**Формально:**

$$\forall \varepsilon > 0 \quad \exists N: \quad \forall n > N \Rightarrow |a_n - A| < \varepsilon$$

**Читается так:**
- Для любого положительного $\varepsilon$ (как угодно малого!)
- Существует номер $N$ (начиная с которого...)
- Для всех $n > N$ выполняется $|a_n - A| < \varepsilon$ (все члены близки к $A$)

---

### Разбор по частям

#### 1. "Для любого $\varepsilon > 0$"

Это значит: **вы** выбираете точность. Хотите $\varepsilon = 0.1$? Пожалуйста. Хотите $\varepsilon = 0.000001$? Без проблем.

**Задача математика:** Показать, что для **любого** вашего выбора $\varepsilon$ можно найти $N$.

#### 2. "Существует номер $N$"

Это **ответ** математика на ваш выбор $\varepsilon$. Чем меньше $\varepsilon$, тем больше может быть $N$.

**Важно:** $N$ зависит от $\varepsilon$, часто пишут $N(\varepsilon)$.

#### 3. "Для всех $n > N$"

Начиная с номера $N$ и **дальше** — **все** члены последовательности (без исключений!) находятся в $\varepsilon$-окрестности от $A$.

---

### Наглядный пример

Рассмотрим $a_n = \frac{1}{n}$ и $A = 0$.

**Утверждение:** $\lim_{n \to \infty} \frac{1}{n} = 0$

**Что это значит:**

| Вы задаёте $\varepsilon$ | Я нахожу $N$ | Проверка |
|---|---|---|
| $\varepsilon = 0.1$ | $N = 10$ | Для $n > 10$: $\frac{1}{n} < 0.1$ ✓ |
| $\varepsilon = 0.01$ | $N = 100$ | Для $n > 100$: $\frac{1}{n} < 0.01$ ✓ |
| $\varepsilon = 0.001$ | $N = 1000$ | Для $n > 1000$: $\frac{1}{n} < 0.001$ ✓ |
| $\varepsilon$ любое | $N = \lceil \frac{1}{\varepsilon} \rceil$ | Для $n > N$: $\frac{1}{n} < \varepsilon$ ✓ |

**Вывод:** Как бы мало ни было $\varepsilon$, я всегда найду такой $N$, что после него все члены будут ближе к нулю, чем $\varepsilon$.

---

### Визуализация

```
             A = 0
             │
    ┌────────┼────────┐  ← "коридор" шириной 2ε
    │        │        │
────┴────────┼────────┴────→
ε   │        │        │  -ε
    │        │        │
    │  • •   │   ••••••  ← члены последовательности a_n
    │ •      │        
    •        │        
            
    ├────────┼────────┤
      N=1    N        
```

**Слева от $N$:** Члены могут быть где угодно  
**Справа от $N$:** Все члены внутри "коридора" $[-\varepsilon, \varepsilon]$

---

### Почему так сложно?

**Житейское "стремится":** "Ну, $\frac{1}{n}$ становится всё меньше..."

**Математическое "стремится":** "Для любой наперёд заданной точности можно гарантировать..."

Разница в **строгости**. Математическое определение:
- Не оставляет "лазеек"
- Работает для любых последовательностей
- Позволяет строго **доказывать** теоремы

</details>

---

<details>
<summary><b>⚖️ В чём разница между "близко" и "стремится"?</b></summary>

### ⚠️ Это НЕ одно и то же!

**"Близко"** — это **статическое** свойство двух чисел  
**"Стремится"** — это **динамическое** свойство бесконечной последовательности

---

### "Близко" = Одномоментное сравнение

**Определение:** Два числа $x$ и $A$ **близки**, если:

$$|x - A| < \varepsilon$$

**Свойства:**
- Сравниваем **два конкретных числа**
- В **один момент времени**
- Либо близки, либо нет (да/нет)

**Примеры:**

✅ $3.14$ близко к $\pi$ с точностью $\varepsilon = 0.01$  
✅ $100.5$ близко к $100$ с точностью $\varepsilon = 1$  
✗ $5$ НЕ близко к $10$ с точностью $\varepsilon = 1$

---

### "Стремится" = Процесс приближения

**Определение:** Последовательность $a_n$ **стремится** к $A$, если:

$$\forall \varepsilon > 0 \quad \exists N: \quad \forall n > N \Rightarrow |a_n - A| < \varepsilon$$

**Свойства:**
- Рассматриваем **бесконечную последовательность чисел** $a_1, a_2, a_3, \ldots$
- **Процесс**: члены становятся всё ближе к $A$
- Для **любой** точности найдётся момент, после которого **все** члены близки

**Ключевое отличие:** "Стремится" означает, что можно **гарантировать** близость **для любой** точности.

---

### Сравнительная таблица

| Свойство | "Близко" | "Стремится" |
|----------|----------|-------------|
| **Объект** | Два числа: $x$ и $A$ | Последовательность: $a_1, a_2, a_3, \ldots$ и число $A$ |
| **Тип** | Статическое (одномоментное) | Динамическое (процесс) |
| **Время** | В один момент | На бесконечности |
| **Формула** | $\|x - A\| < \varepsilon$ | $\forall \varepsilon > 0 \; \exists N: \forall n > N \Rightarrow \|a_n - A\| < \varepsilon$ |
| **Вопрос** | Близки ли эти два числа? | Приближается ли последовательность к пределу? |
| **Ответ** | Да/Нет (зависит от $\varepsilon$) | Да/Нет (для всех $\varepsilon$) |

---

### Связь между понятиями

**"Стремится" использует "близко" внутри определения:**

> Последовательность $a_n$ **стремится** к $A$ означает:  
> Для любой точности $\varepsilon$ найдётся момент $N$, после которого **все** члены $a_n$ **близки** к $A$ (с этой точностью).

**Аналогия:**

- **"Близко"** — это фотография: "Эти два человека стоят рядом"
- **"Стремится"** — это видео: "Этот человек идёт к цели и в конце концов подойдёт как угодно близко"

---

### Примеры для закрепления

#### Пример 1: Последовательность $a_n = \frac{1}{n}$

**Вопрос 1:** Близко ли $a_{10} = 0.1$ к $0$ с точностью $\varepsilon = 0.2$?  
**Ответ:** ✅ Да, $|0.1 - 0| = 0.1 < 0.2$ (это вопрос о "близко")

**Вопрос 2:** Стремится ли последовательность $a_n = \frac{1}{n}$ к $0$?  
**Ответ:** ✅ Да, потому что для любого $\varepsilon$ найдётся $N$ (например, $N = \lceil \frac{1}{\varepsilon} \rceil$), после которого все члены близки к $0$ (это вопрос о "стремится")

#### Пример 2: Последовательность $b_n = (-1)^n$

$$b_1 = -1, \; b_2 = 1, \; b_3 = -1, \; b_4 = 1, \ldots$$

**Вопрос 1:** Близко ли $b_{10} = 1$ к $1$ с точностью $\varepsilon = 0.1$?  
**Ответ:** ✅ Да, $|1 - 1| = 0 < 0.1$ (отдельный член может быть близок)

**Вопрос 2:** Стремится ли последовательность $b_n = (-1)^n$ к какому-то числу?  
**Ответ:** ✗ Нет, потому что члены "прыгают" между $-1$ и $1$, не приближаясь ни к какому пределу (последовательность НЕ стремится)

---

### Вывод

**"Близко"** = разовое сравнение двух чисел  
**"Стремится"** = гарантия близости для всей бесконечной последовательности, начиная с некоторого момента

**Важно:** Отдельные члены последовательности могут быть близки к числу, но вся последовательность при этом может НЕ стремиться к нему (как в примере с $(-1)^n$).

</details>

---

<details>
<summary><b>💡 Идея доказательства (интуитивно)</b></summary>

## Что говорит теорема?

**Простыми словами:** Если две последовательности стремятся к своим пределам, то их сумма стремится к сумме пределов.

**Пример:**
- Последовательность $a_n = \frac{1}{n}$ стремится к $0$
- Последовательность $b_n = \frac{1}{n^2}$ стремится к $0$
- Тогда $(a_n + b_n) = \frac{1}{n} + \frac{1}{n^2}$ тоже стремится к $0 + 0 = 0$

### Почему это неочевидно?

На первый взгляд кажется: "Ну конечно, это же очевидно!" Но в математике нужно **строго доказать**, что:
- Если $a_n$ "почти равно" $A$
- И $b_n$ "почти равно" $B$
- То $(a_n + b_n)$ "почти равно" $(A + B)$

Проблема в том, что "почти равно" — это не точный термин. Нужно работать с формальным определением через $\varepsilon$ и $N$.

---

## Стратегия доказательства (4 шага)

**Задача:** Показать, что $(a_n + b_n)$ можно сделать как угодно близко к $(A + B)$.

### 1. Перегруппировка слагаемых

$$|(a_n + b_n) - (A + B)| = |(a_n - A) + (b_n - B)|$$

**Расшифровка:**
- **Левая часть:** $|(a_n + b_n) - (A + B)|$ — расстояние от **суммы членов** $(a_n + b_n)$ до **суммы пределов** $(A + B)$
- **Правая часть:** $|(a_n - A) + (b_n - B)|$ — модуль суммы двух отклонений:
  - $(a_n - A)$ — отклонение $a_n$ от своего предела $A$
  - $(b_n - B)$ — отклонение $b_n$ от своего предела $B$

**Зачем?** Выделить величины $(a_n - A)$ и $(b_n - B)$, про которые мы УЖЕ ЗНАЕМ (по условию), что они малы.

<details>
<summary>⚠️ Вопрос: "Это же просто перестановка слагаемых, зачем это делать?"</summary>

**Ответ:** Да, алгебраически тривиально! Но **СМЫСЛ** — показать структуру задачи.

**До:** $|(a_n + b_n) - (A + B)|$ — непонятно, как использовать условия

**После:** $|(a_n - A) + (b_n - B)|$ — видим отклонения, которые мы контролируем!

**Аналогия:** $17 + 25 = (20 - 3) + (20 + 5) = 40 + 2 = 42$  
Перегруппировка выделяет удобную структуру.

</details>

### 2. Неравенство треугольника

$$|(a_n - A) + (b_n - B)| \leq |a_n - A| + |b_n - B|$$

⚠️ **Важно:** Переход от $|x + y|$ к $|x| + |y|$ — это **неравенство**, а не равенство!

**Расшифровка правой части:**
- $|a_n - A|$ — **расстояние от $a_n$ до $A$**
- $|b_n - B|$ — **расстояние от $b_n$ до $B$**
- $|a_n - A| + |b_n - B|$ — **сумма этих двух расстояний**

**Смысл:** Модуль суммы ≤ сумма модулей (геометрический факт — правило треугольника).

**Зачем?** 
- Левую часть $|(a_n - A) + (b_n - B)|$ сложно оценить напрямую
- Правую часть $|a_n - A| + |b_n - B|$ можем оценить по условию задачи!
- Неравенство связывает то, что нужно доказать, с тем, что мы знаем

### 3. Трюк с $\frac{\varepsilon}{2}$

Нам нужно, чтобы вся сумма была меньше $\varepsilon$. Поэтому делим "бюджет погрешности" пополам:

- Требуем: $|a_n - A| < \frac{\varepsilon}{2}$
- Требуем: $|b_n - B| < \frac{\varepsilon}{2}$

Тогда:
$$|a_n - A| + |b_n - B| < \frac{\varepsilon}{2} + \frac{\varepsilon}{2} = \varepsilon$$

**Зачем делим пополам?** Чтобы сумма двух маленьких величин дала ровно нужный $\varepsilon$ ✓

### 4. Синхронизация (выбор общего N)

- $a_n$ становится близко к $A$ начиная с номера $N_1$
- $b_n$ становится близко к $B$ начиная с номера $N_2$
- Берём $N = \max(N_1, N_2)$ — тогда при $n > N$ **одновременно** выполняются оба условия!

**Зачем максимум?** Чтобы "подождать", пока **обе** последовательности успокоятся.

<details>
<summary>Подробнее: почему берём максимум?</summary>

**Проблема:** Две последовательности могут "успокаиваться" в разное время:
- Для $a_n$: существует номер $N_1$, после которого $|a_n - A| < \frac{\varepsilon}{2}$
- Для $b_n$: существует номер $N_2$, после которого $|b_n - B| < \frac{\varepsilon}{2}$

**Вопрос:** С какого номера выполняются **ОБА** условия одновременно?

**Решение:** Берём $N = \max(N_1, N_2)$ — **больший** из двух номеров.

**Почему это работает?**

Если $n > N = \max(N_1, N_2)$, то автоматически:
- $n > N_1$ (потому что $N \geq N_1$) → выполняется $|a_n - A| < \frac{\varepsilon}{2}$ ✓
- $n > N_2$ (потому что $N \geq N_2$) → выполняется $|b_n - B| < \frac{\varepsilon}{2}$ ✓

Значит, **оба** неравенства выполнены одновременно!

---

**Конкретный пример:**

Пусть $\varepsilon = 0.1$, тогда нужно $\frac{\varepsilon}{2} = 0.05$.

Для $a_n = \frac{1}{n}$:
- Нужно $\frac{1}{n} < 0.05$
- Это выполняется при $n > 20$
- Значит, $N_1 = 20$

Для $b_n = \frac{1}{n^2}$:
- Нужно $\frac{1}{n^2} < 0.05$
- Это выполняется при $n > \sqrt{20} \approx 4.47$
- Значит, $N_2 = 5$

**Берём $N = \max(20, 5) = 20$**

Что это означает?
- При $n > 20$ **обе** последовательности одновременно в нужной близости:
  - $a_{21} = \frac{1}{21} \approx 0.048 < 0.05$ ✓
  - $b_{21} = \frac{1}{441} \approx 0.0023 < 0.05$ ✓

**Наглядно:**

```
     N₂=5        N₁=20
      ↓            ↓
─────●────────────●──────────→ n
     │            │
     │            └─ a_n "успокоилась"
     └─ b_n "успокоилась"
     
Берём N = 20, тогда после этого момента ОБЕ успокоились!
```

**Вывод:** Максимум гарантирует, что мы подождали **достаточно долго** для обеих последовательностей.

</details>

---

## Визуальная схема

```
Что нужно доказать              Что мы знаем
      ↓                                ↓
|(a_n + b_n) - (A + B)| < ε    |a_n - A| < ε/2
                                |b_n - B| < ε/2

Шаг 1: Перегруппировка ↓
    |(a_n - A) + (b_n - B)|
    
Шаг 2: Неравенство треугольника ↓
    |a_n - A| + |b_n - B|
    
Шаг 3: Подстановка ↓
    ε/2 + ε/2 = ε  ✓
```

---

<details>
<summary>📊 Конкретный пример с числами</summary>

Пусть $a_n = \frac{1}{n} \to 0$ и $b_n = \frac{1}{n^2} \to 0$. При $n = 10$:

| Величина | Значение | Смысл |
|----------|----------|-------|
| $a_{10}$ | $0.1$ | Член первой последовательности |
| $b_{10}$ | $0.01$ | Член второй последовательности |
| $a_{10} + b_{10}$ | $0.11$ | **Сумма членов** |
| $A + B$ | $0$ | **Сумма пределов** |
| Отклонение суммы | $0.11$ | $\|(0.11) - 0\| = 0.11$ |
| Сумма отклонений | $0.11$ | $0.1 + 0.01 = 0.11$ ✓ |

**Вывод:** Если каждое отклонение мало, то и их сумма мала!

</details>

---

## Наглядная аналогия

Представьте:
- Вы идёте к точке $A$ (ваше расстояние $|a_n - A|$)
- Ваш друг идёт к точке $B$ (его расстояние $|b_n - B|$)
- Вопрос: на каком расстоянии от $(A, B)$ будет пара?

**Ответ:** Общее расстояние ≤ сумма индивидуальных расстояний (неравенство треугольника).

Если оба приблизились на $\frac{\varepsilon}{2}$, то пара приблизилась на $\varepsilon$.

</details>

---

## Формальное доказательство

**Дано:**
- $\lim_{n \to \infty} a_n = A$, то есть $\forall \varepsilon_1 > 0 \quad \exists N_1: \quad \forall n > N_1 \Rightarrow |a_n - A| < \varepsilon_1$
- $\lim_{n \to \infty} b_n = B$, то есть $\forall \varepsilon_2 > 0 \quad \exists N_2: \quad \forall n > N_2 \Rightarrow |b_n - B| < \varepsilon_2$

**Доказать:** 

$$\forall \varepsilon > 0 \quad \exists N: \quad \forall n > N \Rightarrow |(a_n + b_n) - (A + B)| < \varepsilon$$

**Доказательство:**

Пусть $\varepsilon > 0$ — произвольное положительное число.

Выберем $\varepsilon_1 = \varepsilon_2 = \frac{\varepsilon}{2}$.

Тогда:
- $\exists N_1$: для всех $n > N_1$ выполняется $|a_n - A| < \frac{\varepsilon}{2}$
- $\exists N_2$: для всех $n > N_2$ выполняется $|b_n - B| < \frac{\varepsilon}{2}$

Возьмем $N = \max(N_1, N_2)$.

Тогда для всех $n > N$ одновременно выполняются оба неравенства.

Рассмотрим:

$$|(a_n + b_n) - (A + B)| = |(a_n - A) + (b_n - B)|$$

По **неравенству треугольника** $|x + y| \leq |x| + |y|$:

$$|(a_n - A) + (b_n - B)| \leq |a_n - A| + |b_n - B|$$

Так как $n > N$, то:

$$|a_n - A| + |b_n - B| < \frac{\varepsilon}{2} + \frac{\varepsilon}{2} = \varepsilon$$

Следовательно:

$$|(a_n + b_n) - (A + B)| < \varepsilon$$

Это выполняется для любого $\varepsilon > 0$, следовательно, по определению:

$$\lim_{n \to \infty} (a_n + b_n) = A + B$$

**Что и требовалось доказать.** ∎

---

## Ключевые идеи

1. **Трюк с делением ε пополам** — берем $\frac{\varepsilon}{2}$ для каждой последовательности, чтобы их сумма дала $\varepsilon$
2. **Неравенство треугольника** — основной инструмент для оценки суммы модулей
3. **Максимум из N₁ и N₂** — обеспечивает одновременное выполнение обоих условий
